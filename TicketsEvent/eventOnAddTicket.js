const { Client } require('discord.js').ticketEvents()

// events: Add Ticket //
/*
* use event.AddTicketOn('Ticket by ${user.tag.name}') 
* class.end
*/
function AddTicketOn(textchannel){
  client.addTicket => {
   name: 'Metadata', // Metadata Events
   host: 'event.ticketJS.chat', // Host TicketJS
   ip: 'event.ticketsJS.chat/request/ip' // IP TicketsEvent 
}
let client = ReadFile$client.map
return 'Successfully Created Ticket.map'

}
